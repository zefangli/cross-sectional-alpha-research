# Raw vendor exports and generated research data stay local.
crsp/
data/raw/
data/processed/
data/interim/
data/raw_hash.txt
data/cleaning_run*.log
data/research_panel_run*.log
data/factor_*_run*.log
data/week*_run*.log

# Python and notebook noise.
__pycache__/
*.py[cod]
.venv/
.ipynb_checkpoints/

# Local secrets and operating-system files.
.env
.DS_Store
Thumbs.db
data/week4_run*.log
reports/post_fix_run*.log

# CRSP-derived series are never published: these files carry the CRSP
# value-weighted market return alongside the book's own daily P&L, and the
# held_events files carry PERMNO-level CRSP returns. Summary statistics,
# protocols and figures stay tracked; these do not. All are regenerable from
# the raw export.
daily_*.csv
**/daily_*.csv
**/audit_daily.csv
**/final_daily.csv
**/held_events.csv
